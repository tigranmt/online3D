<div id="fileInfo" >
    <div class="accordion"  id="infoList"data-bind="foreach: {data : modelsInformation}">
        <div class="accordion-group" >
            <div class="accordion-heading" >
                <div class="accordion-toggle" data-toggle="collapse" data-parent="#infoList" data-bind="attr:{href: href}, 
                                style:{ 'backgroundColor': isVisible() === true ? 'transparent' : 'rgba(250, 34, 86, 0.45)' }">
                    <span id="index" class="badge" data-bind="text:$index, click:click.bind($data), style:{ 'background-color': color}">
                        </span>
                    <span class="label label-info" data-bind="text:fileName"></span>
                </div>
            </div>
            <div  data-bind="attr:{id:$index}" class="accordion-body collapse in">
                <div class="accordion-inner">
                    <span class="label label-inverse" data-bind="text: fileSize"></span>
                    <span class="label label-inverse" data-bind="text: faceCount"></span>
                    <span class="label label-inverse" data-bind="text: vertexCount"></span>
                </div>
            </div>
        </div>       
    </div>
</div>

<div class="navbar"style="
    opacity: 0.9;
    z-index: 100;
    position: relative;
">
    <div class="navbar-inner">    
     <div class="container-fluid">
        <div href="#" class="brand" style="
                    font-size: 41px;
                    font-weight: bold;
                    left: -25px;
                    position: relative;
                    color: #3a87ad;">
            Online3D
        </div>
        <div class="nav-collapse collapse navbar-responsive-collapse">
            <ul  class="nav">
                <li class="dropdown">             
                    <a href="#" data-toggle="dropdown" class="dropdown-toggle">Access<b class="caret"></b></a>      
                    <ul class="dropdown-menu" id="access_ul" data-bind="foreach: access_menu">                     
                       <li>
                           <a data-bind="attr:{id:id}, text:text, click: function(e) { callback(); }, visible: itemVisible" href="#" ></a>
                       </li>
                    </ul>
                </li>
            </ul>

            <ul  class="nav">
                <li class="dropdown">             
                    <a href="#" data-toggle="dropdown" class="dropdown-toggle">Save<b class="caret"></b></a>      
                    <ul class="dropdown-menu" id="save_ul" data-bind="foreach: save_menu">                     
                       <li>
                           <a data-bind="attr:{id:id}, text:text, click: function(e) { callback(); }" href="#" ></a>
                       </li>
                    </ul>
                </li>
            </ul>

            <ul  class="nav">
                <li class="dropdown">             
                    <a href="#" data-toggle="dropdown" class="dropdown-toggle">View<b class="caret"></b></a>      
                    <ul class="dropdown-menu" id="view_ul" data-bind="foreach: view_menu">                     
                       <li>
                           <a data-bind="attr:{id:id}, text:text, click: function(e) { callback(); }" href="#" ></a>
                       </li>
                    </ul>
                </li>
            </ul>
        </div>

       
     </div>

   </div>   
</div>




@*hiddent field for holding the model(s) unique ID that has to be loaded *@
@if ((@ViewBag.ID) != null) {
    <p id="bag" style="visibility:hidden">@(ViewBag.ID)</p>
}


<div id="3DArea">
  
</div>

<div id="basicPanel"  class="btn-group btn-group-vertical">
    
    <div id="innerbasicPanel" data-bind="foreach: buttons">
        <button class="btn btn-block" data-bind="click: function(e) { callback(); }" >
            <img class="imageOnButton" data-bind="attr:{id:id, src:src}" />
        </button>
    </div>
</div>




<div id="progress" ></div>
